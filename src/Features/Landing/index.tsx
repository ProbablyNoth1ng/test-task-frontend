import { Header } from "./Header"
import { Nav } from "./Nav"
import { OpenDeals } from "./OpenDeals/index"

export const LandingFeatures = () => {
    return (
        <>
            <Nav/>
            <Header />
            <OpenDeals />
            
        </>
    )
}